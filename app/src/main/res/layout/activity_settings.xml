<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_settings"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.home.erwin.homework.Activity.SettingsActivity">

    <TextView
        android:text="@string/school_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/usual_text_size"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:id="@+id/school_list" />

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/school_list"
        android:layout_alignParentStart="true"
        android:layout_alignBottom="@+id/settingsSpace"
        android:id="@+id/schoolList" />

    <Space
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_alignParentStart="true"
        android:id="@+id/settingsSpace" />

    <Button
        android:text="@string/delete_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/settingsSpace"
        android:layout_alignParentStart="true"
        android:textColor="#FFF"
        android:background="@drawable/regularbutton"
        android:layout_marginTop="8dp"
        android:id="@+id/deleteAllEntry" />

    <Button
        android:text="New"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/deleteAllEntry"
        android:layout_alignParentEnd="true"
        android:textColor="#FFF"
        android:background="@drawable/regularbutton"
        android:id="@+id/newSchool"
        android:elevation="0dp" />

    <Button
        android:text="Done"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:id="@+id/done"
        android:background="@color/colorPrimary"
        android:textSize="@dimen/usual_button_text_size"
        android:textColor="#FFF"
        android:layout_alignParentEnd="true"
        android:layout_toEndOf="@+id/settingsSpace" />

</RelativeLayout>
